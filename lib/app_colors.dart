import 'package:flutter/material.dart';

class AppColors {
  // Mengubah kode hex menjadi format Color Flutter (tambahkan 0xFF di depan)
  static const Color primary = Color(0xFF002D72);
  static const Color secondary = Color(0xFFC5A059);
  static const Color tertiary = Color(0xFF10B981);
  static const Color neutral = Color(0xFF64748B);
  
  // Background terang (putih sedikit keabu-abuan agar nyaman di mata)
  static const Color backgroundLight = Color(0xFFF8FAFC); 

  static const Color surface = Colors.white;
}